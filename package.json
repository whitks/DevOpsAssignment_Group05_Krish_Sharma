{
  "name": "devops-sample-app",
  "version": "1.0.0",
  "description": "Dependency-free Node.js sample app used by the DevOps pipeline examples",
  "private": true,
  "license": "MIT",
  "main": "server.js",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "node server.js",
    "test": "node --test",
    "lint": "echo \"No linter configured - add eslint or biome if needed\""
  },
  "engines": {
    "node": ">=20"
  }
}